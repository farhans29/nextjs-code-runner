"use client";
import React, { useEffect, useRef } from "react";
import Editor, { OnMount } from "@monaco-editor/react";
interface CodeEditorProps { language?: string; value?: string; onChange?: (value: string | undefined) => void; stdin?: string; onStdinChange?: (value: string) => void; onRunCode?: () => void; isMaximized?: boolean; onToggleMaximize?: () => void; height?: string; }
const getFileName = (language: string) => ({ c: "main.c", cpp: "main.cpp", go: "main.go", rust: "main.rs", typescript: "main.ts", javascript: "main.js", python: "main.py", java: "Main.java", csharp: "Program.cs", html: "index.html" }[language] ?? `main.${language}`);
export default function CodeEditor({ language = "javascript", value, onChange, stdin = "", onStdinChange, onRunCode, isMaximized = false, onToggleMaximize, height = "520px" }: CodeEditorProps) { const runCodeRef = useRef(onRunCode); useEffect(() => { runCodeRef.current = onRunCode; }, [onRunCode]); const handleMount: OnMount = (editor, monaco) => { editor.addCommand(monaco.KeyMod.CtrlCmd | monaco.KeyCode.Enter, () => runCodeRef.current?.()); }; return <section className="workspace-panel" style={{ height }} aria-label="Code editor"><header className="panel-header"><div className="panel-title"><span className="window-dots" aria-hidden="true"><i /><i /><i /></span><span className="file-name">{getFileName(language)}</span></div><div className="panel-state"><span>Ctrl / ⌘ + Enter</span>{onToggleMaximize && <button type="button" className="icon-button" onClick={onToggleMaximize} aria-pressed={isMaximized} aria-label={isMaximized ? "Restore workspace" : "Maximize editor"}>{isMaximized ? "↙" : "↗"}</button>}</div></header><div className="editor-body"><Editor height="100%" language={language} value={value} onChange={onChange} onMount={handleMount} theme="vs-dark" options={{ minimap:{enabled:false}, fontSize:14, lineHeight:22, padding:{top:16,bottom:16}, scrollBeyondLastLine:false, wordWrap:"on", renderLineHighlight:"all", fontFamily:"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace", fontLigatures:true, smoothScrolling:true, cursorBlinking:"smooth" }} /></div><div className="stdin-area"><label className="stdin-label" htmlFor="stdin"><span>Standard input</span><span>Optional program input</span></label><textarea id="stdin" value={stdin} onChange={(event) => onStdinChange?.(event.target.value)} onKeyDown={(event) => { if ((event.metaKey || event.ctrlKey) && event.key === "Enter") { event.preventDefault(); onRunCode?.(); } }} rows={2} spellCheck={false} placeholder="For example: 10 20 30" /></div></section>; }